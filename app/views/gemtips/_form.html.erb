<%= form_with(model: gemtip, local: true) do |form| %>
  <% if gemtip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gemtip.errors.count, "error") %> prohibited this gemtip from being saved:</h2>

      <ul>
      <% gemtip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tipnumb %>
    <%= form.number_field :tipnumb, id: :gemtip_tipnumb %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
